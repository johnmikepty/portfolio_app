---
import '@/styles/global.css?5'
import '@/styles/tinosfont.css?5'

interface Props {
    title?: string
    pageClass?: string  // 'resume-page' | 'portfolio-page'
    lang?: string
}

const { title = 'Portfolio', pageClass = '', lang = 'en' } = Astro.props
---

<!DOCTYPE html>
<html lang={lang}>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta http-equiv="x-ua-compatible" content="ie=edge">

        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/4.1.1/normalize.min.css" />
        <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.6.3/css/font-awesome.min.css" />

        <title>{title}</title>
    </head>
    <body class={pageClass}>
        <main>
            <slot />
        </main>
    </body>
</html>

<style>
    /* Resume layout: dark bg body, white paper content */
    body.resume-page {
        display: flex;
        flex-direction: column;
        align-items: center;
        background: #080c14;
        min-height: 100vh;
    }

    body.resume-page main {
        background: white;
        margin: 0 auto;
        width: 8.5in;
        padding: 0.15in 0.15in;
    }

    @media not print {
        body.resume-page main {
            outline: 1px solid #0004;
            margin-top: 80px;
            margin-bottom: 3rem;
        }
    }

    /* Portfolio layout: unrestricted */
    body.portfolio-page {
        display: block;
        width: 100%;
        max-width: 100%;
    }

    body.portfolio-page main {
        width: 100%;
        max-width: 100%;
        margin: 0;
        padding: 0;
    }
</style>
